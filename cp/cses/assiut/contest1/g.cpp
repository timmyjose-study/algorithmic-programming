#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  unsigned long long n, m, k;
  cin >> n >> m >> k;

  if (n == 0 || k == 0) {
    cout << 0 << "\n";
  } else {
  }

  return 0;
}